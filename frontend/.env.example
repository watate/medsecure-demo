# =============================================================================
# MedSecure Frontend — Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values:
#   cp .env.example .env
# =============================================================================

# --- Better Auth ---
# Required: random secret used for encryption and hashing (generate with: openssl rand -hex 32)
BETTER_AUTH_SECRET=
# Required: base URL of the frontend app
BETTER_AUTH_URL=http://localhost:3000

# --- Backend API ---
# URL of the FastAPI backend
NEXT_PUBLIC_API_URL=http://localhost:8000

# --- Seed Script (used with: npm run seed) ---
# These are only needed when running the seed script, not at runtime.
# SEED_EMAIL=admin@example.com
# SEED_PASSWORD=your-secure-password
# SEED_NAME=Admin
